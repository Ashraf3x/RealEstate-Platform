@model RealEstate.Application.DTOs.PropertyDto
@{
    Layout = "_Layout";
    ViewData["Title"] = "Property Details";
}

<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-md-7">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-5">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h3 class="fw-bold mb-0">@Model.Title</h3>
                        <span class="badge @(Model.Status == "Active" ? "bg-success" : "bg-secondary") fs-6">
                            @Model.Status
                        </span>
                    </div>
                    <p class="text-muted mb-4">@Model.Description</p>
                    <hr />
                    <div class="row g-3 mb-4">
                        <div class="col-6">
                            <div class="bg-light rounded p-3 text-center">
                                <p class="text-muted small mb-1">Price Per Share</p>
                                <h5 class="fw-bold mb-0">$@Model.PricePerShare</h5>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="bg-light rounded p-3 text-center">
                                <p class="text-muted small mb-1">Total Shares</p>
                                <h5 class="fw-bold mb-0">@Model.TotalShares</h5>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="bg-light rounded p-3 text-center">
                                <p class="text-muted small mb-1">Location</p>
                                <h5 class="fw-bold mb-0">@Model.Location</h5>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="bg-light rounded p-3 text-center">
                                <p class="text-muted small mb-1">Listed</p>
                                <h5 class="fw-bold mb-0">@Model.CreatedAt.ToString("MMM yyyy")</h5>
                            </div>
                        </div>
                    </div>
                    <div class="d-flex gap-2">
                        <a asp-controller="SaleListings" asp-action="GetByPropertyId"
                           asp-route-propertyId="@Model.PropertyId"
                           class="btn btn-success flex-grow-1">View Listings</a>
                        <a asp-action="Index" class="btn btn-outline-secondary">Back</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>